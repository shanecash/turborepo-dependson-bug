{
  "name": "turborepo-dependson-bug",
  "version": "1.0.0",
  "scripts": {
    "pre-build": "node -e \"console.log('Ran pre-build')\"",
    "build": "turbo run build",
    "pre-dev": "node -e \"console.log('Ran pre-dev')\"",
    "dev": "turbo run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "license": "ISC",
  "dependencies": {
    "turbo": "^1.13.3-canary.0"
  }
}
